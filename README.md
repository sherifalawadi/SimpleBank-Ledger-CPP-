# SimpleBank-Ledger-CPP-
A C++ based Bank Ledger system that manages accounts and transactions using a command line interface. It features secure data persistence in CSV format, transaction logging with ISO timestamps, and a strict no-overdraft policy for reliable financial tracking.
